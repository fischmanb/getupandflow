/* Quiet matching-phase note carried by the goals and rhythm sections while
   the client is unassigned: what they keep current here steers the match,
   alongside personal fit. One component so both notes stay visually
   identical; gone once a coach is assigned, never in mirror view. */
export function MatchingNote({ children }) {
  return <p className="home-matching-note">{children}</p>;
}
